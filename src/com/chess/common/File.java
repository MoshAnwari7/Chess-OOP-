package com.chess.common;

/**
 * Represents the file (column) of a chessboard.
 *
 * <p>
 * Files are labeled from A to H, corresponding to the eight vertical columns
 * on a standard chessboard. The ordinal value of each enum constant (0–7)
 * aligns directly with board column indexing, where A=0, B=1, ..., H=7.
 * </p>
 */
public enum File {
    A, B, C, D, E, F, G, H
}
